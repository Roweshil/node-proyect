string a = "hola";
string b = "mundo";


public static class Ejercicio
{
    private void Main(string a, string b)
    {
        string flores = new string[3];

        flores[0 / contenido, 1 / contenido, 2 / contenido]

        listas enlazadas

        (contenido / referencia ) -> (contenido / referencia)->  (contenido / referencia)

        Mouse{
            int peso = 1;
            float version = 0.2;
            string marca = "logitech";
            bool inalambrico = true;
        };

        public Mouse mouseXispa;

        mouseItzia = Mouse(200, 1.1, "logitech", true);
        mouseRowe = Mouse(150, 1.0, "hp", false);
        mouseAbram = Mouse(100, 2.0, "dell", true);

        if (version < 1.1) {
            Debug.Log("El mouse es de version alta");
        }


        if (inalambrico == true) {
            Debug.Log("El mouse es inalambrico");
        } else {
            Debug.Log("El mouse es con cable");
        }

string[] marcasMouse = new string["logitech", "hp", "dell"];
        

        foreach(string marcasMouse)
        {
            //hace cosas
        }

        for (int i = 0; i < 3; i++)
        {
            Debug.Log("Marca del mouse: " + marcasMouse[2]);
        }


        int luces = 4;

        if (1 = 10)
        {
            codigo
            } if (1 = 2)
        {

        } if (algo)
        {

        }


        do {
            Debug.Log("bienvenido al altar");
            i++;
        } while (luces < 5);

        if (luces = 5)
        {
            Debug.Log("has completado las luces");
        }

        luces = null;

        switch (luces)
        {
            case roja:
                // solo tienes una luz
                break;

            case azul:
                // solo tienes 2
                break;

            case verde:

                break;

            default:
                // Código si no coincide con ningún caso
                break;
        }







        Debug.Log("Tu mouse es de la marca: " + marcaMouse); // Tu mouse es de la marca: logitech


        string c = a + b
        Console.WriteLine(c); //holamundo
    }


    private void funcion()
    {
        
    }








oncolision {
    tag("rio")
    vector3 = 
}




}